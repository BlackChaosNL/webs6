import { TournamentViewHostDirective } from './tournament-view-host.directive';

describe('TournamentViewHostDirective', () => {
  it('should create an instance', () => {
    const directive = new TournamentViewHostDirective();
    expect(directive).toBeTruthy();
  });
});
